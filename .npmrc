# eslint-config-next loads its plugins via the legacy eslintrc resolver, which
# needs them resolvable from the project root. pnpm's isolated store hides
# transitive deps, so hoist the ESLint ecosystem to node_modules root.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=@next/eslint-plugin-next
